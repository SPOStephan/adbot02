{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo 'prebuilt by sync workflow'",
  "buildCommand": "echo 'prebuilt by sync workflow'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
