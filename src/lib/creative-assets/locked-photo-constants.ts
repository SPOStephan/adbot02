/** Phase 3: exactly one locked photo per job (no scale/crop of the photo). */
export const PHASE3_MAX_LOCKED_PHOTOS = 1;

export const LOCKED_PHOTO_COMPOSE_VERSION =
  "adbot-locked-photo-compose-v1" as const;
